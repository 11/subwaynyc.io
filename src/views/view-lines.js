import { LitElement, html, css } from 'lit-element'

class ViewLines extends LitElement {
  constructor() {
    super()
  }

  onConnectedCallback() {
    super.onConnectedCallback()
  }

  render() {
    return html``
  }
}

customElements.define('view-lines', ViewLines)
